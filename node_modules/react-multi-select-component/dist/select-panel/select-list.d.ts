/// <reference types="react" />
import { Option } from "../lib/interfaces";
interface ISelectListProps {
    focusIndex: number;
    ItemRenderer?: Function;
    options: Option[];
    value: Option[];
    onChange: (selected: Option[]) => void;
    onClick: Function;
    disabled?: boolean;
}
declare const SelectList: ({ value, onChange, disabled, ItemRenderer, options, focusIndex, onClick, }: ISelectListProps) => JSX.Element;
export default SelectList;
